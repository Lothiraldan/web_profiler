============
Installation
============

At the command line::

    $ easy_install web_profiler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv web_profiler
    $ pip install web_profiler
